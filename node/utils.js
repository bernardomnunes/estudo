console.log("utils.js");

const firstName = "Bernardo"; // A variavel nao vai para o app.js apenas o clg dele
const lastName = "Nunes";

const add = (a, b) => a + b;
module.exports = { firstName, lastName, add };
