const string = "Ser ou nao ser, eis a questao";

const comecaComSer = string.startsWith("nao ser", 7);
console.log(comecaComSer);

const terminaComQuestao = string.endsWith("questao");
console.log(terminaComQuestao);
